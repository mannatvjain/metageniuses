# Dependencies for the experiment scripts in experiments/.
# The extraction and SAE training pipelines have additional GPU dependencies;
# see pyproject.toml for those.

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
pandas>=2.0
umap-learn>=0.5
hdbscan>=0.8
